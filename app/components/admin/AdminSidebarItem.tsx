interface AdminSidebarItem{
    selected?:boolean,
    name:string,
    icon:IconType
}
const AdminSidebarItem = ({admin}) => {
  return (
    <div>
    </div>
  );
};

export default AdminSidebarItem;